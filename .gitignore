# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
release/
*.local

# Test artifacts
test-results
playwright-report
blob-report
playwright/.cache
coverage
.scratch

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Third-party source assets (e.g. the Darth Maul .blend + textures): large, not redistributable.
downloads/

# Local engine-evaluation scratch (Unreal startup investigation): ~153MB of generated artifacts,
# including a 118MB CachedAssetRegistry blob, crash minidumps and full-res screenshots. Kept on
# disk because the Unreal direction is still live, but none of it belongs in the repo.
diagnostics/

# Python bytecode cache from running scripts/art/*.py via Blender's bundled Python.
__pycache__/
